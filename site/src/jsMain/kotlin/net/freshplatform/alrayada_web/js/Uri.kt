package net.freshplatform.alrayada_web.js

/**
 * Refer to [encodeURIComponent() docs](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/encodeURIComponent).
 *
 * @return A new string representing the provided [uriComponent] encoded as a URI component.
 * @param uriComponent A string to be encoded as a URI component (a path, query string, fragment, etc.).
 * */
external fun encodeURIComponent(uriComponent: String): String
